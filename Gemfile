# webrick ships with some Ruby installs (e.g. Homebrew) but not all — declaring
# it here makes hosted deploys work. Locally, plain `ruby server.rb` still works.
source 'https://rubygems.org'

gem 'webrick', '~> 1.9'
